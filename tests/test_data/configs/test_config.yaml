name: "Configuration for tests"
description: "Do not use, will most likely not work"

device: "cpu"
print_prio: "DEBUG"
is_training: off

test_config:
    module_name: "Dummy"
    kwargs:
        more_data: true
        even_more_data: [1,2,3,4]

# Modules
